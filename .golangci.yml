version: '2'
linters:
  default: all
  disable:
    - depguard
    - exhaustive
    - exhaustruct
    - exhaustruct_v5
    - funcorder
    - gomodguard
    - noinlineerr
    - wsl
  exclusions:
    rules:
      - linters:
          - err113
          - funlen
          - staticcheck
          - testpackage
        path: (.+)_test\.go
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
