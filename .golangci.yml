issues:
  exclude-rules:
    - linters:
        - funlen
        - staticcheck
        - goerr113
        - testpackage
      path: '(.+)_test\.go'

linters:
  enable-all: true
  disable:
    - maligned
    - scopelint
    - interfacer
    - golint
    - exhaustivestruct
    - exhaustruct
    - nlreturn
    - varcheck
    - nosnakecase
    - structcheck
    - deadcode
    - ifshort
    - exhaustive
    - depguard
    - tagalign

run:
  timeout: 3m